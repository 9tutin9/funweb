{
  "name": "funweb",
  "version": "1.0.0",
  "private": true,
  "description": "Static site with a serverless endpoint for brief form (Resend)",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site – no build'",
    "start": "vercel dev"
  },
  "dependencies": {
    "resend": "^3.2.0"
  }
}
